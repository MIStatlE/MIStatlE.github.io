# 让 notes 目录下的所有 .qmd 默认带这些 Front Matter 字段（可被单篇覆盖）
title-block-banner: false
freeze: auto

# 通用默认：没有设置 image 时使用占位图；默认分类 tags
image: ../../images/tool-card.png
categories: [probability]
draft: false

# （可选）默认显示在卡片中的信息（配合 index.qmd 的 listing.fields）
description: "Notes on probability and stochastic analysis."
